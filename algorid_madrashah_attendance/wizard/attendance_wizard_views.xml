<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Attendance Report Wizard Form -->
    <record id="view_attendance_report_wizard_form" model="ir.ui.view">
        <field name="name">algorid.attendance.report.wizard.form</field>
        <field name="model">algorid.attendance.report.wizard</field>
        <field name="arch" type="xml">
            <form string="Attendance Report">
                <group>
                    <group>
                        <field name="report_type" widget="radio"/>
                        <field name="date_from"/>
                        <field name="date_to"/>
                    </group>
                    <group>
                        <field name="export_format" widget="radio"/>
                    </group>
                </group>
                <group invisible="report_type != 'student'">
                    <group>
                        <field name="class_id"/>
                        <field name="section_id"/>
                        <field name="student_id"/>
                    </group>
                </group>
                <group invisible="report_type != 'teacher'">
                    <group>
                        <field name="teacher_id"/>
                    </group>
                </group>
                <group invisible="not csv_file">
                    <field name="csv_file" filename="csv_filename"/>
                    <field name="csv_filename" invisible="1"/>
                </group>
                <footer>
                    <button name="action_generate_report" string="Generate Report" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Attendance Report Wizard Action -->
    <record id="action_attendance_report_wizard" model="ir.actions.act_window">
        <field name="name">Attendance Report</field>
        <field name="res_model">algorid.attendance.report.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>

